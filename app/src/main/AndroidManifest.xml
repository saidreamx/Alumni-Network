<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sanjeev.alumninetwork">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".splashScreen">
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".profileInfo.onePerson"
            android:theme = "@style/MyMaterialTheme.Base"/>
        <activity android:name=".signUp.logIn"/>
        <activity android:name=".signUp.collectionLoginSignup" />
        <activity android:name=".peopleList.showAlumniList"/>
        <activity android:name=".signUp.mail_Developer"/>
        <activity android:name=".profileInfo.projectAdd"/>




    </application>

</manifest>
